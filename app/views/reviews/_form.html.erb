  <%= form_for [@restaurant, @review] do |f| %>
    <%= f.label :content %>
    <%= f.text_field  :content %>
    <%= f.label :rating %>
    <%= f.select(:rating, options_for_select([0,1,2,3,4,5])) %>
    <%= f.submit "New reviewizzle"%>
  <% end %>
